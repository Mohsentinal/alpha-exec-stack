# alpha-exec-stack requirements (research pipeline)
# Pinning loosely to keep installs smooth on Windows; you can tighten later if you want.

numpy>=1.24
pandas>=2.0

polars>=0.20
pyarrow>=14.0

scikit-learn>=1.3

matplotlib>=3.7

loguru>=0.7
python-dateutil>=2.8

websockets>=11.0
orjson>=3.9
tenacity>=8.2
